// Legacy AniList admin module intentionally disabled.
// The MangaMorph admin now uses admin-anilist-direct-sync.js as the single
// source of truth for AniList matching, profile updates and cover syncing.
// Keeping this file as a harmless module avoids stale HTML/cache references
// from registering a second set of click handlers on the same controls.
export {};
